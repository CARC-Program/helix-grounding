# Normalize line endings in the repository to LF, check out native on Windows.
# Without this git warns on every add ("LF will be replaced by CRLF") and diffs
# get noisy the moment a second machine touches the repo.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
